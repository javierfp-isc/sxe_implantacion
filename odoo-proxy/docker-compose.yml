services:
  odoo-1:
    image: odoo:19-auto     
    container_name: odoo-proxy-1
    hostname: odoo-1
    command:
      - /bin/sh
      - -c
      - |
          chown -R odoo:odoo /opt/odoo/.local/share/Odoo
          /etc/init.d/odoo.sh start
          exec bash -c 'while true; do sleep 3600; done'
    volumes:
      - ~/odoo/src:/opt/odoo/src
      - odoo_data:/opt/odoo/.local/share/Odoo

  odoo-2:
    image: odoo:19-auto     
    container_name: odoo-proxy-2
    hostname: odoo-2
    command:
      - /bin/sh
      - -c
      - |
          chown -R odoo:odoo /opt/odoo/.local/share/Odoo
          /etc/init.d/odoo.sh start
          exec bash -c 'while true; do sleep 3600; done'
    volumes:
      - ~/odoo/src:/opt/odoo/src
      - odoo_data:/opt/odoo/.local/share/Odoo

  nginx:
    image: nginx:alpine
    container_name: odoo-nginx-proxy
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - odoo-1
      - odoo-2
  postgres:
    image: odoo-postgres:17
    container_name: odoo-postgres-proxy
    hostname: odoo-postgres
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: odoo
      POSTGRES_PASSWORD: abc123.
    ports:
      - "15432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
  odoo_data: